# Base URL of the backend API (see the companion backend project).
# If unreachable, the app gracefully falls back to realistic demo data
# for every read so the UI stays fully explorable.
VITE_API_BASE_URL=http://localhost:4000/api
